import 'package:flutter/widgets.dart';

class PrinterInfoWidget extends StatelessWidget {
  const PrinterInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('No Printer');
  }
}
